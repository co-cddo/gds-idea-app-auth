version: 2
updates:
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
    # Only create PRs for security vulnerabilities, not routine version bumps
    open-pull-requests-limit: 5
    groups:
      # Group all non-security updates together if they do appear
      dependencies:
        patterns:
          - "*"
